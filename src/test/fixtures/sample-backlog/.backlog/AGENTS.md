# Backlog system instructions

Tasks live as individual markdown files with YAML frontmatter in `tasks/`.
Epics live in `epics/`, sprint definitions in `sprints/`. `backlog.md` is the
status-grouped dashboard. Keep `backlog.md` in sync when statuses change.

This file is ignored by the Backlog Explorer parser — it is guidance for AI
agents only.
